using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImageMoney : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SeedManager.grantReward();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
